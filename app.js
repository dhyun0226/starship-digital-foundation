const toast=document.querySelector('[data-toast]');const backdrop=document.querySelector('[data-dialog-backdrop]');const openButton=document.querySelector('[data-dialog-open]');const closeButton=document.querySelector('[data-dialog-close]');const confirmButton=document.querySelector('[data-dialog-confirm]');let timer;function showToast(title='저장했습니다',message='변경사항이 반영되었습니다.'){toast.querySelector('b').textContent=title;toast.querySelector('span').textContent=message;toast.classList.add('is-open');clearTimeout(timer);timer=setTimeout(()=>toast.classList.remove('is-open'),2600)}function openDialog(){backdrop.hidden=false;closeButton.focus()}function closeDialog(){backdrop.hidden=true;openButton.focus()}document.querySelector('[data-toast-trigger]').addEventListener('click',()=>showToast());openButton.addEventListener('click',openDialog);closeButton.addEventListener('click',closeDialog);confirmButton.addEventListener('click',()=>{closeDialog();showToast('제출했습니다','인사담당자에게 평가가 전달되었습니다.')});backdrop.addEventListener('click',e=>{if(e.target===backdrop)closeDialog()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!backdrop.hidden)closeDialog()});
